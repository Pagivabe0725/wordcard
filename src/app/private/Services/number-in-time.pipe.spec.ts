import { NumberInTimePipe } from './number-in-time.pipe';

describe('NumberInTimePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberInTimePipe();
    expect(pipe).toBeTruthy();
  });
});
